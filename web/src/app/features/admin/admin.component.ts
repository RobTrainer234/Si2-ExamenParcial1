import { CurrencyPipe } from '@angular/common';
import { HttpErrorResponse } from '@angular/common/http';
import { Component, inject } from '@angular/core';
import { FormsModule, FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms';

import { AdminService } from '../../core/admin/admin.service';
import { AdminUser, Branch, Category, City, Collection, Color, ImageInput, InventoryRow, ProductDetail, ProductSummary, Season, Size, SizeSystem, Supplier, SupplyOffer, SupplyStatus } from '../../core/models/admin.models';

type Section = 'overview' | 'users' | 'locations' | 'masters' | 'suppliers' | 'seasons' | 'products';

@Component({
  standalone: true,
  imports: [CurrencyPipe, FormsModule, ReactiveFormsModule],
  template: `
    <div class="admin-layout">
      <aside class="admin-sidebar">
        <p class="eyebrow">Workspace</p><h1>Control<br><em>center.</em></h1>
        <nav class="admin-nav">
          <button [class.current]="section === 'overview'" (click)="section = 'overview'">Resumen</button>
          <button [class.current]="section === 'users'" (click)="section = 'users'">Usuarios</button>
          <button [class.current]="section === 'locations'" (click)="section = 'locations'">Ciudades y sucursales</button>
          <button [class.current]="section === 'masters'" (click)="section = 'masters'">Catálogo base</button>
          <button [class.current]="section === 'suppliers'" (click)="section = 'suppliers'">Proveedores</button>
          <button [class.current]="section === 'seasons'" (click)="section = 'seasons'">Temporadas</button>
          <button [class.current]="section === 'products'" (click)="section = 'products'">Productos</button>
        </nav>
      </aside>
      <section class="admin-content">
        @if (error) { <p class="error admin-alert">{{ error }}</p> }
        @if (section === 'overview') {
           <div class="overview-workspace"><div class="admin-heading overview-heading"><div><p class="eyebrow">Administración</p><h2>Resumen operativo</h2><p class="admin-subtitle">Una lectura rápida del estado actual de tu tienda y sus operaciones principales.</p></div><span class="overview-date">HOY · {{ overviewDate }}</span></div>
            <div class="overview-hero"><div><span class="panel-index">PANEL DE CONTROL</span><h3>Todo lo que importa,<br><em>en un solo lugar.</em></h3><p>Gestiona usuarios, inventario y puntos de venta desde un espacio centralizado.</p></div><span class="material-symbols-outlined">dashboard</span></div>
            <div class="overview-stats"><button type="button" (click)="section = 'users'"><span class="overview-stat-icon material-symbols-outlined">group</span><span class="overview-stat-copy"><strong>{{ users.length }}</strong><small>Usuarios registrados</small><em>{{ activeUserCount }} activos</em></span><span class="material-symbols-outlined overview-arrow">arrow_outward</span></button><button type="button" (click)="section = 'products'"><span class="overview-stat-icon material-symbols-outlined">inventory_2</span><span class="overview-stat-copy"><strong>{{ products.length }}</strong><small>Productos</small><em>{{ activeProductCount }} publicados</em></span><span class="material-symbols-outlined overview-arrow">arrow_outward</span></button><button type="button" (click)="section = 'locations'"><span class="overview-stat-icon material-symbols-outlined">storefront</span><span class="overview-stat-copy"><strong>{{ branches.length }}</strong><small>Sucursales</small><em>{{ activeBranches.length }} activas</em></span><span class="material-symbols-outlined overview-arrow">arrow_outward</span></button><button type="button" (click)="section = 'products'"><span class="overview-stat-icon material-symbols-outlined">tune</span><span class="overview-stat-copy"><strong>{{ variantCount }}</strong><small>Variantes</small><em>Configuradas en catálogo</em></span><span class="material-symbols-outlined overview-arrow">arrow_outward</span></button></div>
            <div class="overview-lower"><section class="overview-panel"><div class="overview-panel-heading"><div><span class="panel-index">01</span><h3>Accesos rápidos</h3></div></div><div class="quick-actions"><button type="button" (click)="section = 'products'"><span class="material-symbols-outlined">add_box</span><span><strong>Registrar producto</strong><small>Añade una nueva pieza al catálogo</small></span><span class="material-symbols-outlined">arrow_forward</span></button><button type="button" (click)="section = 'users'"><span class="material-symbols-outlined">person_add</span><span><strong>Crear usuario</strong><small>Invita a alguien a tu equipo</small></span><span class="material-symbols-outlined">arrow_forward</span></button><button type="button" (click)="section = 'locations'"><span class="material-symbols-outlined">add_business</span><span><strong>Añadir sucursal</strong><small>Expande tus puntos de venta</small></span><span class="material-symbols-outlined">arrow_forward</span></button></div></section><section class="overview-panel overview-note"><span class="material-symbols-outlined">tips_and_updates</span><div><span class="panel-index">NOTA</span><h3>Un espacio para avanzar</h3><p>Los cambios que realices aquí se reflejan directamente en el catálogo público y en la operación de tu tienda.</p></div></section></div></div>
        }
         @if (section === 'users') {
           <div class="admin-panel user-edit-panel"><div class="variant-heading"><div><span class="panel-index">EDITAR ACCESO</span><h3>Modificar usuario y rol</h3><p>Actualiza los datos, rol y proveedor asociado sin perder sus sucursales.</p></div></div><label>Usuario<select [value]="editingUserId" (change)="selectUserForEdit($any($event.target).value)"><option value="">Selecciona un usuario</option>@for (user of users; track user.id) { <option [value]="user.id">{{ user.first_name }} {{ user.last_name }} · {{ roleLabel(user.role) }}</option> }</select></label>@if (editingUser) { <form [formGroup]="userEditForm" (ngSubmit)="saveUserEdit()" class="user-edit-form"><label>Nombres<input formControlName="first_name"></label><label>Apellidos<input formControlName="last_name"></label><label>Teléfono<input formControlName="phone"></label><label>Rol<select formControlName="role"><option value="CLIENT">Cliente</option><option value="ADMIN">Administrador</option><option value="BRANCH_MANAGER">Encargado de sucursal</option><option value="CASHIER">Cajero</option><option value="SUPPLIER">Proveedor</option></select></label>@if (userEditForm.controls.role.value === 'SUPPLIER') { <label>Proveedor asociado<select formControlName="supplier_id"><option value="">Selecciona proveedor</option>@for (supplier of activeSuppliers; track supplier.id) { <option [value]="supplier.id">{{ supplier.trade_name }}</option> }</select></label> }<button class="button button-dark" [disabled]="userEditForm.invalid || (userEditForm.controls.role.value === 'SUPPLIER' && !userEditForm.controls.supplier_id.value)">Guardar cambios</button></form> }</div>
           <div class="users-workspace"><div class="admin-heading users-heading"><div><p class="eyebrow">Administración</p><h2>Usuarios</h2><p class="admin-subtitle">Administra quién puede acceder a la operación y qué puede hacer dentro de ella.</p></div><button class="button button-dark" type="button" (click)="showUserForm = !showUserForm">{{ showUserForm ? 'Cerrar formulario' : '+ Nuevo usuario' }}</button></div>
           <div class="user-stats"><div><span class="material-symbols-outlined">group</span><strong>{{ users.length }}</strong><small>Registrados</small></div><div><span class="material-symbols-outlined">verified_user</span><strong>{{ activeUserCount }}</strong><small>Activos</small></div><div><span class="material-symbols-outlined">admin_panel_settings</span><strong>{{ adminUserCount }}</strong><small>Administradores</small></div></div>
           @if (showUserForm) { <form [formGroup]="userForm" (ngSubmit)="createUser()" class="user-create-form"><div class="user-form-heading"><span class="material-symbols-outlined">person_add</span><div><span class="panel-index">NUEVO ACCESO</span><h3>Registrar usuario</h3><small>Completa los datos para crear un nuevo acceso a la plataforma.</small></div></div><div class="user-form-grid"><label>Nombres<input placeholder="Ej. Ana" formControlName="first_name"></label><label>Apellidos<input placeholder="Ej. Prueba" formControlName="last_name"></label><label>Correo electrónico<input placeholder="nombre@empresa.com" type="email" formControlName="email"></label><label>Teléfono<input placeholder="+591 ..." formControlName="phone"></label><label>Contraseña<input placeholder="Mínimo 8 caracteres" type="password" formControlName="password"></label><label>Rol<select formControlName="role"><option value="CLIENT">Cliente</option><option value="ADMIN">Administrador</option><option value="BRANCH_MANAGER">Encargado de sucursal</option><option value="CASHIER">Cajero</option><option value="SUPPLIER">Proveedor</option></select></label>@if (userForm.controls.role.value === 'SUPPLIER') { <label>Proveedor asociado<select formControlName="supplier_id"><option value="">Selecciona proveedor</option>@for (supplier of activeSuppliers; track supplier.id) { <option [value]="supplier.id">{{ supplier.trade_name }}</option> }</select></label> }</div><div class="user-form-footer"><span>El usuario podrá iniciar sesión inmediatamente.</span><button class="button button-dark" [disabled]="userForm.invalid">Crear usuario</button></div></form> }
            <div class="user-toolbar"><label class="location-search"><span class="material-symbols-outlined">search</span><input [(ngModel)]="userQuery" placeholder="Buscar por nombre o correo..." aria-label="Buscar usuarios"></label><div class="status-filters"><button type="button" [class.selected]="userFilter === 'all'" (click)="userFilter = 'all'">Todos</button><button type="button" [class.selected]="userFilter === 'active'" (click)="userFilter = 'active'">Activos</button><button type="button" [class.selected]="userFilter === 'inactive'" (click)="userFilter = 'inactive'">Inactivos</button></div></div>
            @if (branchAssignableUsers.length) { <div class="admin-panel branch-assignment-panel"><div class="variant-heading"><div><span class="panel-index">SUCURSALES</span><h3>Asignar sucursales operativas</h3><p>Define dónde pueden trabajar los encargados y cajeros.</p></div></div><div class="branch-assignment-grid"><label>Usuario<select [value]="branchAssignmentUserId" (change)="selectBranchAssignmentUser($any($event.target).value)"><option value="">Selecciona un usuario</option>@for (user of branchAssignableUsers; track user.id) { <option [value]="user.id">{{ user.first_name }} {{ user.last_name }} · {{ roleLabel(user.role) }}</option> }</select></label><div><span class="bulk-label">Sucursales asignadas</span><div class="size-checks">@for (branch of activeBranches; track branch.id) { <label [class.checked]="branchAssignmentIds.includes(branch.id)"><input type="checkbox" [checked]="branchAssignmentIds.includes(branch.id)" (change)="toggleBranchAssignment(branch.id)"><span>{{ branch.name }}</span></label> }</div></div></div><div class="bulk-variant-footer"><small>{{ branchAssignmentIds.length }} sucursal(es) seleccionada(s)</small><button class="button button-dark" type="button" [disabled]="!branchAssignmentUserId" (click)="saveBranchAssignment()">Guardar asignación</button></div></div> }
           <div class="users-table-card"><div class="user-table-heading"><div><span class="panel-index">DIRECTORIO</span><h3>Usuarios del sistema</h3></div><span class="panel-count">{{ filteredUsers.length }} resultados</span></div><div class="table-wrap"><table><thead><tr><th>Usuario</th><th>Correo electrónico</th><th>Rol</th><th>Estado</th><th></th></tr></thead><tbody>@for (user of filteredUsers; track user.id) { <tr><td><div class="user-name-cell"><span class="user-avatar">{{ user.first_name.charAt(0) }}{{ user.last_name.charAt(0) }}</span><span><strong>{{ user.first_name }} {{ user.last_name }}</strong><small>ID de usuario #{{ user.id }}</small></span></div></td><td>{{ user.email }}<small class="user-phone">{{ user.phone }}</small></td><td><span class="role-pill">{{ roleLabel(user.role) }}</span></td><td><span class="status-pill" [class.active]="user.is_active">{{ user.is_active ? 'Activo' : 'Inactivo' }}</span></td><td><button class="small-button" type="button" (click)="toggleUser(user)">{{ user.is_active ? 'Desactivar' : 'Activar' }}</button></td></tr> } @empty { <tr><td colspan="5"><div class="user-empty"><span class="material-symbols-outlined">group_off</span><strong>No hay usuarios con este filtro</strong><small>Prueba otra búsqueda o registra un nuevo acceso.</small></div></td></tr> }</tbody></table></div></div></div>
        }
        @if (section === 'locations') {
          <div class="location-workspace">
            <div class="admin-heading location-heading"><div><p class="eyebrow">Estructura física</p><h2>Ciudades y sucursales</h2><p class="admin-subtitle">Organiza los puntos donde vive tu colección y mantén su estado actualizado.</p></div><div class="location-actions"><button class="button button-light" type="button" (click)="showCityForm = !showCityForm">{{ showCityForm ? 'Cerrar' : '+ Nueva ciudad' }}</button><button class="button button-dark" type="button" (click)="showBranchForm = !showBranchForm">{{ showBranchForm ? 'Cerrar' : '+ Nueva sucursal' }}</button></div></div>
            <div class="location-stats"><button type="button" (click)="locationFilter = 'all'"><strong>{{ cities.length }}</strong><span>Ciudades registradas</span></button><button type="button" (click)="locationFilter = 'active'"><strong>{{ activeCities.length }}</strong><span>Ciudades activas</span></button><button type="button" (click)="locationFilter = 'branches'"><strong>{{ activeBranches.length }}</strong><span>Sucursales activas</span></button></div>
            @if (showCityForm || showBranchForm) { <div class="location-forms">@if (showCityForm) { <form [formGroup]="cityForm" (ngSubmit)="createCity()" class="location-form"><div class="form-step"><span>01</span><div><strong>Nueva ciudad</strong><small>Añade una ciudad para organizar sus sucursales.</small></div></div><div class="location-form-row"><input placeholder="Ej. Santa Cruz" formControlName="name" aria-label="Nombre de ciudad"><button class="button button-dark" [disabled]="cityForm.invalid">Añadir ciudad</button></div></form> } @if (showBranchForm) { <form [formGroup]="branchForm" (ngSubmit)="createBranch()" class="location-form"><div class="form-step"><span>02</span><div><strong>Nueva sucursal</strong><small>Completa los datos del punto de venta físico.</small></div></div><div class="location-form-grid"><select formControlName="city_id"><option value="">Selecciona una ciudad</option>@for (city of activeCities; track city.id) { <option [value]="city.id">{{ city.name }}</option> }</select><input placeholder="Nombre de sucursal" formControlName="name"><input placeholder="Dirección" formControlName="address"><input placeholder="Teléfono" formControlName="phone"><button class="button button-dark" [disabled]="branchForm.invalid">Añadir sucursal</button></div></form> }</div> }
            <div class="location-toolbar"><label class="location-search"><span class="material-symbols-outlined">search</span><input [(ngModel)]="locationQuery" placeholder="Buscar ciudad o sucursal..." aria-label="Buscar ciudad o sucursal"></label><div class="status-filters"><button type="button" [class.selected]="locationFilter === 'all'" (click)="locationFilter = 'all'">Todo</button><button type="button" [class.selected]="locationFilter === 'active'" (click)="locationFilter = 'active'">Activas</button><button type="button" [class.selected]="locationFilter === 'inactive'" (click)="locationFilter = 'inactive'">Inactivas</button></div></div>
            <div class="location-grid"><section class="admin-panel city-panel"><div class="panel-heading"><div><span class="panel-index">01</span><h3>Ciudades</h3></div><span class="panel-count">{{ filteredCities.length }}</span></div><div class="city-list">@for (city of filteredCities; track city.id) { <button type="button" class="city-card" [class.selected]="selectedCityId === city.id" (click)="selectCity(city)"><span class="city-card-icon material-symbols-outlined">location_city</span><span class="city-card-copy"><strong>{{ city.name }}</strong><small>{{ branchCount(city.id) }} {{ branchCount(city.id) === 1 ? 'sucursal' : 'sucursales' }}</small></span><span class="status-dot" [class.active]="city.is_active"></span><span class="material-symbols-outlined city-arrow">chevron_right</span></button> } @empty { <p class="location-empty">No encontramos ciudades con ese criterio.</p> }</div></section><section class="admin-panel branch-panel"><div class="panel-heading"><div><span class="panel-index">02</span><h3>Sucursales</h3></div><span class="panel-count">{{ filteredBranches.length }}</span></div><p class="selected-location">{{ selectedCityName }}</p><div class="branch-list">@for (branch of filteredBranches; track branch.id) { <article class="branch-card"><div class="branch-card-top"><span class="branch-card-icon material-symbols-outlined">storefront</span><div><strong>{{ branch.name }}</strong><small>{{ branch.city_name }}</small></div><span class="status-pill" [class.active]="branch.is_active">{{ branch.is_active ? 'Activa' : 'Inactiva' }}</span></div><div class="branch-details"><span><span class="material-symbols-outlined">pin_drop</span>{{ branch.address }}</span><span><span class="material-symbols-outlined">phone</span>{{ branch.phone }}</span></div><div class="branch-card-actions"><button class="small-button" type="button" (click)="toggleBranch(branch)">{{ branch.is_active ? 'Desactivar' : 'Activar' }}</button></div></article> } @empty { <div class="location-empty location-empty-large"><span class="material-symbols-outlined">storefront</span><strong>No hay sucursales para mostrar</strong><small>Selecciona una ciudad o añade una nueva sucursal.</small></div> }</div></section></div>
           </div>
        }
         @if (section === 'masters') {
           <div class="admin-panel master-edit-panel"><div class="variant-heading"><div><span class="panel-index">EDITAR CATÁLOGO BASE</span><h3>Modificar atributos</h3><p>Selecciona una categoría, talla o color para actualizar sus datos.</p></div></div><div class="master-edit-toolbar"><label>Tipo<select [(ngModel)]="masterEditType" (change)="masterEditId = ''; editingMaster = null"><option value="categories">Categoría</option><option value="sizes">Talla</option><option value="colors">Color</option></select></label><label>Elemento<select [value]="masterEditId" (change)="selectMasterForEdit($any($event.target).value)"><option value="">Selecciona un elemento</option>@for (item of masterEditItems; track item.id) { <option [value]="item.id">{{ item.name }}</option> }</select></label></div>@if (editingMaster) { <form [formGroup]="masterEditForm" (ngSubmit)="saveMasterEdit()" class="master-edit-form"><label>Nombre<input formControlName="name"></label>@if (masterEditType === 'categories') { <label>Descripción<input formControlName="description"></label> } @if (masterEditType === 'sizes') { <label>Tipo<select formControlName="size_type"><option value="ALPHA">Alfabética</option><option value="NUMERIC">Numérica</option><option value="ONE_SIZE">Talla única</option></select></label><label>Orden<input type="number" min="0" formControlName="sort_order"></label> } @if (masterEditType === 'colors') { <label>Código hexadecimal<input formControlName="hex_code" placeholder="#000000"></label> }<button class="button button-dark" [disabled]="masterEditForm.invalid">Guardar cambios</button></form> }</div>
          <div class="masters-workspace">
            <div class="admin-heading masters-heading"><div><p class="eyebrow">Datos administrables</p><h2>Catálogo base</h2><p class="admin-subtitle">Define las piezas de información que alimentan tus productos y variantes.</p></div></div>
            <div class="masters-stats"><div><span class="material-symbols-outlined">category</span><strong>{{ activeCategories.length }}</strong><small>Categorías activas</small></div><div><span class="material-symbols-outlined">straighten</span><strong>{{ activeSizes.length }}</strong><small>Tallas disponibles</small></div><div><span class="material-symbols-outlined">palette</span><strong>{{ activeColors.length }}</strong><small>Colores registrados</small></div></div>
            <div class="masters-toolbar"><span>Gestiona tus atributos</span><div class="status-filters"><button type="button" [class.selected]="masterFilter === 'all'" (click)="masterFilter = 'all'">Todo</button><button type="button" [class.selected]="masterFilter === 'active'" (click)="masterFilter = 'active'">Activos</button><button type="button" [class.selected]="masterFilter === 'inactive'" (click)="masterFilter = 'inactive'">Inactivos</button></div></div>
            <div class="master-grid master-grid-enhanced">
              <section class="master-card category-master"><div class="master-card-header"><div><span class="master-icon material-symbols-outlined">category</span><div><h3>Categorías</h3><small>Familias de prendas</small></div></div><span class="master-count">{{ filteredCategories.length }}</span></div><div class="master-card-actions"><label class="master-search"><span class="material-symbols-outlined">search</span><input [(ngModel)]="categoryQuery" placeholder="Buscar categoría" aria-label="Buscar categoría"></label><button class="button button-dark" type="button" (click)="showCategoryForm = !showCategoryForm">{{ showCategoryForm ? 'Cerrar' : '+ Añadir' }}</button></div>@if (showCategoryForm) { <form [formGroup]="categoryForm" (ngSubmit)="createCategory()" class="master-create-form"><input placeholder="Nombre de categoría" formControlName="name"><button class="button button-dark" [disabled]="categoryForm.invalid">Guardar</button></form> }<div class="master-list">@for (category of filteredCategories; track category.id) { <div class="master-item"><span class="master-item-icon material-symbols-outlined">checkroom</span><div><strong>{{ category.name }}</strong><small>{{ category.is_active ? 'Disponible para productos' : 'Oculta del catálogo' }}</small></div><button class="master-switch" [class.on]="category.is_active" type="button" (click)="toggleMaster('categories', category)" [attr.aria-label]="category.is_active ? 'Desactivar categoría' : 'Activar categoría'"><span></span></button></div> } @empty { <p class="master-empty">No hay categorías con este filtro.</p> }</div></section>
              <section class="master-card size-master"><div class="master-card-header"><div><span class="master-icon material-symbols-outlined">straighten</span><div><h3>Tallas</h3><small>Alfabéticas, numéricas y únicas</small></div></div><span class="master-count">{{ filteredSizes.length }}</span></div><div class="master-card-actions"><label class="master-search"><span class="material-symbols-outlined">search</span><input [(ngModel)]="sizeQuery" placeholder="Buscar talla" aria-label="Buscar talla"></label><button class="button button-dark" type="button" (click)="showSizeForm = !showSizeForm">{{ showSizeForm ? 'Cerrar' : '+ Añadir' }}</button></div>@if (showSizeForm) { <form [formGroup]="sizeForm" (ngSubmit)="createSize()" class="master-create-form size-create-form"><input placeholder="Ej. M o 32" formControlName="name"><select formControlName="size_type"><option value="ALPHA">Alfabética</option><option value="NUMERIC">Numérica</option><option value="ONE_SIZE">Talla única</option></select><input type="number" min="0" placeholder="Orden" formControlName="sort_order"><button class="button button-dark" [disabled]="sizeForm.invalid">Guardar</button></form> }<div class="size-list">@for (size of filteredSizes; track size.id) { <button class="size-item" [class.inactive]="!size.is_active" type="button" (click)="toggleMaster('sizes', size)"><strong>{{ size.name }}</strong><span>{{ sizeTypeLabel(size.size_type) }} · {{ size.is_active ? 'Activa' : 'Inactiva' }}</span></button> } @empty { <p class="master-empty">No hay tallas con este filtro.</p> }</div></section>
              <section class="master-card color-master"><div class="master-card-header"><div><span class="master-icon material-symbols-outlined">palette</span><div><h3>Colores</h3><small>Paleta de colección</small></div></div><span class="master-count">{{ filteredColors.length }}</span></div><div class="master-card-actions"><label class="master-search"><span class="material-symbols-outlined">search</span><input [(ngModel)]="colorQuery" placeholder="Buscar color" aria-label="Buscar color"></label><button class="button button-dark" type="button" (click)="showColorForm = !showColorForm">{{ showColorForm ? 'Cerrar' : '+ Añadir' }}</button></div>@if (showColorForm) { <form [formGroup]="colorForm" (ngSubmit)="createColor()" class="master-create-form color-create-form"><input placeholder="Nombre" formControlName="name"><input placeholder="#000000" formControlName="hex_code"><button class="button button-dark" [disabled]="colorForm.invalid">Guardar</button></form> }<div class="master-list">@for (color of filteredColors; track color.id) { <div class="master-item"><i class="color-preview" [style.background]="color.hex_code || '#b7aea3'"></i><div><strong>{{ color.name }}</strong><small>{{ color.hex_code || 'Sin código hexadecimal' }}</small></div><button class="master-switch" [class.on]="color.is_active" type="button" (click)="toggleMaster('colors', color)" [attr.aria-label]="color.is_active ? 'Desactivar color' : 'Activar color'"><span></span></button></div> } @empty { <p class="master-empty">No hay colores con este filtro.</p> }</div></section>
            </div>
          </div>
        }
        @if (section === 'suppliers') {
          <div class="suppliers-workspace">
            <div class="admin-heading suppliers-heading"><div><p class="eyebrow">Cadena de suministro</p><h2>Proveedores</h2><p class="admin-subtitle">Centraliza tus aliados comerciales, sus datos fiscales y canales de contacto.</p></div><button class="button button-dark" type="button" (click)="showSupplierForm = !showSupplierForm">{{ showSupplierForm ? 'Cerrar formulario' : '+ Nuevo proveedor' }}</button></div>
            <div class="supplier-stats"><div><span class="material-symbols-outlined">local_shipping</span><strong>{{ suppliers.length }}</strong><small>Registrados</small></div><div><span class="material-symbols-outlined">verified</span><strong>{{ activeSuppliers.length }}</strong><small>Activos</small></div><div><span class="material-symbols-outlined">mail</span><strong>{{ suppliersWithEmail }}</strong><small>Con contacto</small></div></div>
            @if (showSupplierForm) { <form [formGroup]="supplierForm" (ngSubmit)="createSupplier()" class="supplier-create-form"><div class="supplier-form-heading"><span class="master-icon material-symbols-outlined">business</span><div><strong>Registrar proveedor</strong><small>Los datos fiscales ayudan a identificarlo en las compras.</small></div></div><div class="supplier-form-grid"><label>Nombre comercial<input placeholder="Ej. Textiles Andinos" formControlName="trade_name"></label><label>Razón social<input placeholder="Razón social legal" formControlName="legal_name"></label><label>Identificación fiscal<input placeholder="NIT / identificación" formControlName="tax_id"></label><label>Correo electrónico<input placeholder="contacto@empresa.bo" type="email" formControlName="email"></label><label>Teléfono<input placeholder="+591 ..." formControlName="phone"></label><label>Dirección<input placeholder="Dirección comercial" formControlName="address"></label></div><div class="supplier-form-footer"><span>Todos los campos se pueden editar después.</span><button class="button button-dark" [disabled]="supplierForm.invalid">Guardar proveedor</button></div></form> }
            <div class="supplier-toolbar"><label class="location-search"><span class="material-symbols-outlined">search</span><input [(ngModel)]="supplierQuery" placeholder="Buscar por nombre, NIT o correo..." aria-label="Buscar proveedor"></label><div class="status-filters"><button type="button" [class.selected]="supplierFilter === 'all'" (click)="supplierFilter = 'all'">Todo</button><button type="button" [class.selected]="supplierFilter === 'active'" (click)="supplierFilter = 'active'">Activos</button><button type="button" [class.selected]="supplierFilter === 'inactive'" (click)="supplierFilter = 'inactive'">Inactivos</button></div></div>
             <div class="supplier-list">@for (supplier of filteredSuppliers; track supplier.id) { <article class="supplier-card"><div class="supplier-card-header"><div class="supplier-avatar material-symbols-outlined">business</div><div><strong>{{ supplier.trade_name }}</strong><small>{{ supplier.legal_name || 'Razón social no registrada' }}</small></div><span class="status-pill" [class.active]="supplier.is_active">{{ supplier.is_active ? 'Activo' : 'Inactivo' }}</span></div><div class="supplier-card-body"><div><span class="supplier-label">Identificación fiscal</span><strong>{{ supplier.tax_id || 'No registrada' }}</strong></div><div><span class="supplier-label">Contacto</span><strong>{{ supplier.email || supplier.phone || 'Sin contacto' }}</strong></div><div><span class="supplier-label">Productos asociados</span><strong>{{ supplier.product_ids.length }}</strong></div></div><div class="supplier-card-footer"><span class="supplier-contact"><span class="material-symbols-outlined">call</span>{{ supplier.phone || 'Sin teléfono' }}</span><button class="small-button" type="button" (click)="toggleSupplier(supplier)">{{ supplier.is_active ? 'Desactivar proveedor' : 'Activar proveedor' }}</button></div></article> } @empty { <div class="supplier-empty"><span class="material-symbols-outlined">inventory_2</span><strong>No hay proveedores con este filtro</strong><small>Prueba otra búsqueda o registra un nuevo proveedor.</small></div> }</div>
          </div>
        }
        @if (section === 'seasons') {
          <div class="seasons-workspace">
            <div class="admin-heading seasons-heading"><div><p class="eyebrow">Organización comercial</p><h2>Temporadas y colecciones</h2><p class="admin-subtitle">Planifica el calendario comercial y agrupa las piezas de cada lanzamiento.</p></div><div class="season-actions"><button class="button button-light" type="button" (click)="showSeasonForm = !showSeasonForm">{{ showSeasonForm ? 'Cerrar' : '+ Nueva temporada' }}</button><button class="button button-dark" type="button" (click)="showCollectionForm = !showCollectionForm">{{ showCollectionForm ? 'Cerrar' : '+ Nueva colección' }}</button></div></div>
            <div class="season-stats"><div><span class="material-symbols-outlined">calendar_month</span><strong>{{ seasons.length }}</strong><small>Temporadas</small></div><div><span class="material-symbols-outlined">event_available</span><strong>{{ activeSeasons.length }}</strong><small>Activas</small></div><div><span class="material-symbols-outlined">collections_bookmark</span><strong>{{ collections.length }}</strong><small>Colecciones</small></div></div>
            @if (showSeasonForm || showCollectionForm) { <div class="season-forms">@if (showSeasonForm) { <form [formGroup]="seasonForm" (ngSubmit)="createSeason()" class="season-form"><div class="form-step"><span>01</span><div><strong>Nueva temporada</strong><small>Define el periodo de la próxima colección.</small></div></div><label>Nombre<input placeholder="Ej. Otoño Invierno 2026" formControlName="name"></label><div class="date-grid"><label>Inicio<input type="date" formControlName="starts_on"></label><label>Fin<input type="date" formControlName="ends_on"></label></div><button class="button button-dark" [disabled]="seasonForm.invalid">Guardar temporada</button></form> } @if (showCollectionForm) { <form [formGroup]="collectionForm" (ngSubmit)="createCollection()" class="season-form"><div class="form-step"><span>02</span><div><strong>Nueva colección</strong><small>Asóciala a una temporada activa.</small></div></div><label>Temporada<select formControlName="season_id"><option value="">Selecciona temporada</option>@for (season of activeSeasons; track season.id) { <option [value]="season.id">{{ season.name }}</option> }</select></label><label>Nombre<input placeholder="Ej. Atelier Essentials" formControlName="name"></label><label>Descripción<textarea placeholder="Describe el concepto de la colección" formControlName="description"></textarea></label><button class="button button-dark" [disabled]="collectionForm.invalid">Guardar colección</button></form> }</div> }
            <div class="season-toolbar"><label class="location-search"><span class="material-symbols-outlined">search</span><input [(ngModel)]="seasonQuery" placeholder="Buscar temporada o colección..." aria-label="Buscar temporada o colección"></label><div class="status-filters"><button type="button" [class.selected]="seasonFilter === 'all'" (click)="seasonFilter = 'all'">Todo</button><button type="button" [class.selected]="seasonFilter === 'active'" (click)="seasonFilter = 'active'">Activas</button><button type="button" [class.selected]="seasonFilter === 'inactive'" (click)="seasonFilter = 'inactive'">Inactivas</button></div></div>
            <div class="season-layout"><section class="admin-panel season-panel"><div class="panel-heading"><div><span class="panel-index">01</span><h3>Temporadas</h3></div><span class="panel-count">{{ filteredSeasons.length }}</span></div><div class="season-list">@for (season of filteredSeasons; track season.id) { <button type="button" class="season-card" [class.selected]="selectedSeasonId === season.id" (click)="selectSeason(season)"><span class="season-card-icon material-symbols-outlined">calendar_month</span><span class="season-card-copy"><strong>{{ season.name }}</strong><small>{{ seasonDateRange(season) }}</small><em>{{ collectionCount(season.id) }} {{ collectionCount(season.id) === 1 ? 'colección' : 'colecciones' }}</em></span><span class="status-dot" [class.active]="season.is_active"></span><span class="material-symbols-outlined">chevron_right</span></button> } @empty { <p class="season-empty">No hay temporadas con este filtro.</p> }</div></section><section class="admin-panel collection-panel"><div class="panel-heading"><div><span class="panel-index">02</span><h3>Colecciones</h3></div><span class="panel-count">{{ filteredCollections.length }}</span></div><p class="selected-season">{{ selectedSeasonName }}</p><div class="collection-list">@for (collection of filteredCollections; track collection.id) { <article class="collection-card"><div class="collection-card-header"><span class="collection-icon material-symbols-outlined">collections_bookmark</span><div><strong>{{ collection.name }}</strong><small>{{ collection.season_name }}</small></div><span class="status-pill" [class.active]="collection.is_active">{{ collection.is_active ? 'Activa' : 'Inactiva' }}</span></div><p>{{ collection.description || 'Colección sin descripción.' }}</p><div class="collection-card-footer"><span>{{ collection.is_active ? 'Visible para productos' : 'Oculta del catálogo' }}</span><button class="small-button" type="button" (click)="toggleCollection(collection)">{{ collection.is_active ? 'Desactivar' : 'Activar' }}</button></div></article> } @empty { <div class="season-empty season-empty-large"><span class="material-symbols-outlined">collections_bookmark</span><strong>No hay colecciones para esta temporada</strong><small>Selecciona otra temporada o crea una nueva colección.</small></div> }</div></section></div>
          </div>
        }
        @if (section === 'products') {
          <div class="products-workspace">
            <div class="admin-heading products-heading"><div><p class="eyebrow">Inventario comercial</p><h2>Productos</h2><p class="admin-subtitle">Construye tu catálogo, asigna cada pieza a su colección y controla su disponibilidad.</p></div><button class="button button-dark" type="button" (click)="startProduct()">{{ showProductForm ? 'Cerrar formulario' : '+ Nuevo producto' }}</button></div>
            <div class="product-stats"><div><span class="material-symbols-outlined">inventory_2</span><strong>{{ products.length }}</strong><small>Productos registrados</small></div><div><span class="material-symbols-outlined">check_circle</span><strong>{{ activeProductCount }}</strong><small>Activos en catálogo</small></div><div><span class="material-symbols-outlined">tune</span><strong>{{ variantCount }}</strong><small>Variantes configuradas</small></div></div>
            @if (showProductForm) { <form [formGroup]="productForm" (ngSubmit)="saveProduct()" class="product-create-form"><div class="product-form-header"><div><span class="panel-index">{{ editingProduct ? 'EDITAR' : '01' }}</span><h3>{{ editingProduct ? 'Editar producto' : 'Registrar producto' }}</h3><p>Completa la información esencial y organiza la pieza dentro del catálogo.</p></div><span class="material-symbols-outlined">apparel</span></div><div class="product-form-grid"><div class="product-form-section"><div class="form-section-title"><span>01</span><strong>Identidad del producto</strong></div><div class="product-fields two-columns"><label>Nombre del producto<input placeholder="Ej. Chaqueta Atelier" formControlName="name"></label><label>Código interno<input placeholder="Ej. ATJ-001" formControlName="code"></label><label>Slug comercial<input placeholder="chaqueta-atelier" formControlName="slug"></label><label>Precio de venta<input placeholder="0.00" type="number" step="0.01" formControlName="price"><small class="field-prefix">Bs</small></label><label class="full-field">Descripción<textarea placeholder="Describe materiales, corte o detalles de la pieza" formControlName="description"></textarea></label></div></div><div class="product-form-section"><div class="form-section-title"><span>02</span><strong>Clasificación comercial</strong></div><div class="product-fields"><label>Público<select formControlName="audience"><option value="WOMEN">Mujer</option><option value="MEN">Hombre</option><option value="UNISEX">Unisex</option></select></label><label>Categoría<select formControlName="category_id"><option value="">Selecciona una categoría</option>@for (category of activeCategories; track category.id) { <option [value]="category.id">{{ category.name }}</option> }</select></label><label>Temporada <small class="optional-label">Opcional</small><select formControlName="season_id" (change)="loadProductCollections()"><option value="">Sin temporada</option>@for (season of activeSeasons; track season.id) { <option [value]="season.id">{{ season.name }}</option> }</select></label><label>Colección <small class="optional-label">Opcional</small><select formControlName="collection_id"><option value="">Sin colección</option>@for (collection of productCollections; track collection.id) { <option [value]="collection.id">{{ collection.name }}</option> }</select></label></div></div><div class="product-form-section product-suppliers"><div class="form-section-title"><span>03</span><div><strong>Proveedores</strong><small>Selecciona los aliados que suministran esta pieza.</small></div></div><div class="supplier-choices">@for (supplier of activeSuppliers; track supplier.id) { <label class="supplier-choice" [class.selected]="selectedSupplierIds.includes(supplier.id)"><input type="checkbox" [checked]="selectedSupplierIds.includes(supplier.id)" (change)="toggleSupplierSelection(supplier.id)"><span class="supplier-choice-mark material-symbols-outlined">business</span><span>{{ supplier.trade_name }}</span><span class="material-symbols-outlined check-mark">check</span></label> } @empty { <small class="muted-copy">No hay proveedores activos registrados.</small> }</div></div></div><div class="product-form-footer"><span>Las variantes e imágenes se pueden añadir después de crear el producto.</span><button class="button button-dark" [disabled]="productForm.invalid">{{ editingProduct ? 'Guardar cambios' : 'Crear producto' }}</button></div></form> }
             @if (showProductForm) { <div class="product-size-system-quick"><label>Sistema de tallas<select [value]="productForm.controls.size_system.value" (change)="setProductSizeSystem($any($event.target).value)"><option value="ALPHA">Alfabéticas (XS, S, M, L)</option><option value="NUMERIC">Numéricas (28, 30, 32)</option><option value="ONE_SIZE">Talla única</option></select></label><small>Las variantes nuevas solo aceptarán tallas de este sistema.</small></div> }
             <div class="product-toolbar"><label class="location-search"><span class="material-symbols-outlined">search</span><input [(ngModel)]="productQuery" placeholder="Buscar por nombre, código o categoría..." aria-label="Buscar productos"></label><div class="status-filters"><button type="button" [class.selected]="productFilter === 'all'" (click)="productFilter = 'all'">Todo</button><button type="button" [class.selected]="productFilter === 'active'" (click)="productFilter = 'active'">Activos</button><button type="button" [class.selected]="productFilter === 'inactive'" (click)="productFilter = 'inactive'">Inactivos</button></div></div>
            <div class="product-table-card"><div class="product-table-heading"><div><span class="panel-index">CATÁLOGO</span><h3>Inventario de productos</h3></div><span class="panel-count">{{ filteredProducts.length }} resultados</span></div><div class="table-wrap"><table><thead><tr><th>Producto</th><th>Público</th><th>Código</th><th>Categoría</th><th>Precio</th><th>Variantes</th><th>Estado</th><th></th></tr></thead><tbody>@for (product of filteredProducts; track product.id) { <tr [class.selected-row]="selectedProduct?.id === product.id"><td><button class="product-name-button" type="button" (click)="selectProduct(product)"><span class="product-row-icon material-symbols-outlined">apparel</span><span><strong>{{ product.name }}</strong><small>Ver detalle del producto</small></span></button></td><td><span class="audience-pill audience-{{ product.audience.toLowerCase() }}">{{ audienceLabel(product.audience) }}</span></td><td><code>{{ product.code }}</code></td><td>{{ product.category_name }}</td><td><strong>{{ product.price | currency:'BOB':'symbol':'1.2-2' }}</strong></td><td><span class="variant-count">{{ product.variant_count }}</span></td><td><span class="status-pill" [class.active]="product.is_active">{{ product.is_active ? 'Activo' : 'Inactivo' }}</span></td><td><div class="row-actions"><button class="small-button" type="button" (click)="editProduct(product)">Editar</button><button class="icon-action" type="button" (click)="toggleProduct(product)" [attr.aria-label]="product.is_active ? 'Desactivar producto' : 'Activar producto'"><span class="material-symbols-outlined">{{ product.is_active ? 'visibility_off' : 'visibility' }}</span></button></div></td></tr> } @empty { <tr><td colspan="8"><div class="product-empty"><span class="material-symbols-outlined">inventory_2</span><strong>No hay productos con este filtro</strong><small>Registra tu primer producto o cambia la búsqueda.</small></div></td></tr> }</tbody></table></div></div>
            @if (selectedProduct) { <div class="admin-panel variant-panel"><div class="variant-heading"><div><span class="panel-index">DETALLE</span><h3>Variantes e imágenes</h3><p>{{ selectedProduct.name }} · {{ selectedProduct.code }}</p></div><span class="status-pill" [class.active]="selectedProduct.is_active">{{ selectedProduct.is_active ? 'Activo' : 'Inactivo' }}</span></div><form [formGroup]="variantForm" (ngSubmit)="addVariant()" class="inline-form"><select formControlName="size_id"><option value="">Talla</option>@for (size of activeSizes; track size.id) { <option [value]="size.id">{{ size.name }}</option> }</select><select formControlName="color_id"><option value="">Color</option>@for (color of activeColors; track color.id) { <option [value]="color.id">{{ color.name }}</option> }</select><input placeholder="SKU" formControlName="sku"><button class="button button-dark" [disabled]="variantForm.invalid">Agregar variante</button></form><form [formGroup]="imageForm" (ngSubmit)="uploadSelectedImages()" class="image-upload-form"><div class="image-upload-row"><label class="file-picker"><input type="file" accept="image/jpeg,image/png,image/webp" multiple (change)="selectImageFiles($event)"><span class="material-symbols-outlined">add_photo_alternate</span><span><strong>{{ imageFiles.length ? imageFiles.length + ' foto(s) seleccionada(s)' : 'Seleccionar fotografías' }}</strong><small>JPG, PNG o WebP · máximo 5 MB cada una</small></span></label><label>Orden inicial<input type="number" min="0" formControlName="sort_order"></label><label class="primary-check"><input type="checkbox" formControlName="is_primary"> Imagen principal</label><button class="button button-dark" type="submit" [disabled]="!imageFiles.length || uploadingImages">{{ uploadingImages ? 'Subiendo...' : 'Subir fotografías' }}</button></div>@if (imageFiles.length) { <div class="upload-preview-list">@for (preview of imagePreviewUrls; track preview; let index = $index) { <div><img [src]="preview" [alt]="imageFiles[index].name"><small>{{ imageFiles[index].name }}</small></div> }</div> }</form><div class="image-grid">@for (image of selectedProduct.images; track image.id) { <figure><img [src]="image.image_url" [alt]="selectedProduct.name"><figcaption><span>{{ image.is_primary ? 'Principal' : 'Imagen ' + image.sort_order }}</span><span class="image-actions">@if (!image.is_primary) { <button type="button" (click)="setPrimaryImage(image.id)">Principal</button> }<button type="button" (click)="removeImage(image.id)">Eliminar</button></span></figcaption></figure> }</div></div> }
             @if (selectedProduct) { <div class="admin-panel inventory-panel"><div class="variant-heading"><div><span class="panel-index">STOCK</span><h3>Disponibilidad por sucursal</h3><p>{{ selectedProduct.variants.length ? 'Asigna existencias a cada combinación de talla, color y tienda.' : 'Primero agrega al menos una variante de talla y color para poder asignar stock.' }}</p></div></div><form [formGroup]="inventoryForm" (ngSubmit)="saveInventory()" class="inline-form"><select formControlName="product_variant_id" [disabled]="!selectedProduct.variants.length"><option value="">{{ selectedProduct.variants.length ? 'Variante' : 'Sin variantes disponibles' }}</option>@for (variant of selectedProduct.variants; track variant.id) { <option [value]="variant.id">{{ variant.size_name }} · {{ variant.color_name }} · {{ variant.sku }}</option> }</select><select formControlName="branch_id" [disabled]="!selectedProduct.variants.length"><option value="">Sucursal</option>@for (branch of activeBranches; track branch.id) { <option [value]="branch.id">{{ branch.name }}</option> }</select><input type="number" min="0" placeholder="Cantidad" formControlName="stock_quantity" [disabled]="!selectedProduct.variants.length"><button class="button button-dark" [disabled]="inventoryForm.invalid || !selectedProduct.variants.length">Guardar stock</button></form>@if (inventoryRows.length) { <div class="inventory-summary">@for (item of inventoryRows; track item.id) { <div><strong>{{ item.size_name }} · {{ item.color_name }}</strong><span>{{ item.branch_name }}: {{ item.stock_quantity }}</span></div> }</div> } @else { <p class="muted">Aún no hay existencias registradas para este producto.</p> }</div> }
             @if (selectedProduct) { <div class="admin-panel bulk-variant-panel"><div class="variant-heading"><div><span class="panel-index">TALLAS</span><h3>Agregar varias tallas</h3><p>Marca las tallas disponibles. El sistema generará automáticamente el SKU.</p></div></div><div class="bulk-variant-controls"><label>Color<select [value]="variantForm.controls.color_id.value" (change)="setVariantColor($any($event.target).value)"><option value="">Selecciona un color</option>@for (color of activeColors; track color.id) { <option [value]="color.id">{{ color.name }}</option> }</select></label><div class="size-checklist"><span class="bulk-label">Tallas disponibles</span><div class="size-checks">@for (size of activeSizes; track size.id) { <label [class.checked]="isSizeSelected(size.id)"><input type="checkbox" [checked]="isSizeSelected(size.id)" (change)="toggleSize(size.id)"><span>{{ size.name }}</span></label> }</div></div></div><div class="bulk-variant-footer"><small>{{ selectedSizeIds.length ? selectedSizeIds.length + ' talla(s) seleccionada(s)' : 'Selecciona al menos una talla' }}</small><button class="button button-dark" type="button" [disabled]="bulkVariantInvalid" (click)="addSelectedVariants()">Agregar {{ selectedSizeIds.length || '' }} variante(s)</button></div></div> }
            </div>
          }
        </section>
    </div>
  `,
})
export class AdminComponent {
  private readonly api = inject(AdminService);
  private readonly fb = inject(FormBuilder);
  section: Section = 'overview'; error = '';
  locationQuery = ''; locationFilter: 'all' | 'active' | 'inactive' | 'branches' = 'all'; selectedCityId: number | null = null;
   userQuery = ''; userFilter: 'all' | 'active' | 'inactive' = 'all'; editingUserId = ''; editingUser: AdminUser | null = null;
   categoryQuery = ''; sizeQuery = ''; colorQuery = ''; masterFilter: 'all' | 'active' | 'inactive' = 'all'; masterEditType: 'categories' | 'sizes' | 'colors' = 'categories'; masterEditId = ''; editingMaster: Category | Size | Color | null = null;
  supplierQuery = ''; supplierFilter: 'all' | 'active' | 'inactive' = 'all';
  productQuery = ''; productFilter: 'all' | 'active' | 'inactive' = 'all';
  seasonQuery = ''; seasonFilter: 'all' | 'active' | 'inactive' = 'all'; selectedSeasonId: number | null = null; branchAssignmentUserId = ''; branchAssignmentIds: number[] = [];
  users: AdminUser[] = []; cities: City[] = []; branches: Branch[] = []; categories: Category[] = []; sizes: Size[] = []; colors: Color[] = []; suppliers: Supplier[] = []; seasons: Season[] = []; collections: Collection[] = []; productCollections: Collection[] = []; products: ProductSummary[] = [];
  selectedProduct: ProductDetail | null = null; selectedSupplierIds: number[] = []; editingProduct: ProductSummary | null = null; inventoryRows: InventoryRow[] = []; selectedSizeIds: number[] = [];
  imageFiles: File[] = []; imagePreviewUrls: string[] = []; uploadingImages = false;
  showUserForm = false; showSupplierForm = false; showProductForm = false; showCityForm = false; showBranchForm = false; showCategoryForm = false; showSizeForm = false; showColorForm = false; showSeasonForm = false; showCollectionForm = false;
   readonly userForm = this.fb.nonNullable.group({ first_name: ['', Validators.required], last_name: ['', Validators.required], email: ['', [Validators.required, Validators.email]], phone: ['', Validators.required], password: ['', [Validators.required, Validators.minLength(8)]], role: ['CLIENT', Validators.required], supplier_id: [''] });
   readonly userEditForm = this.fb.nonNullable.group({ first_name: ['', Validators.required], last_name: ['', Validators.required], phone: ['', Validators.required], role: ['CLIENT', Validators.required], supplier_id: [''] });
  readonly cityForm = this.fb.nonNullable.group({ name: ['', Validators.required] }); readonly branchForm = this.fb.nonNullable.group({ city_id: ['', Validators.required], name: ['', Validators.required], address: ['', Validators.required], phone: ['', Validators.required] });
   readonly categoryForm = this.fb.nonNullable.group({ name: ['', Validators.required] }); readonly sizeForm = this.fb.nonNullable.group({ name: ['', Validators.required], size_type: ['ALPHA' as SizeSystem, Validators.required], sort_order: [0, [Validators.required, Validators.min(0)]] }); readonly colorForm = this.fb.nonNullable.group({ name: ['', Validators.required], hex_code: [''] });
   readonly masterEditForm = this.fb.nonNullable.group({ name: ['', Validators.required], description: [''], size_type: ['ALPHA' as SizeSystem], sort_order: [0, [Validators.required, Validators.min(0)]], hex_code: [''] });
  readonly supplierForm = this.fb.nonNullable.group({ trade_name: ['', Validators.required], legal_name: [''], tax_id: [''], email: ['', Validators.email], phone: [''], address: [''] });
  readonly seasonForm = this.fb.nonNullable.group({ name: ['', Validators.required], starts_on: [''], ends_on: [''] }); readonly collectionForm = this.fb.nonNullable.group({ season_id: ['', Validators.required], name: ['', Validators.required], description: [''] });
  readonly productForm = this.fb.nonNullable.group({ category_id: ['', Validators.required], season_id: [''], collection_id: [''], audience: ['UNISEX', Validators.required], size_system: ['ALPHA' as SizeSystem, Validators.required], code: ['', Validators.required], name: ['', Validators.required], slug: ['', Validators.required], price: [0, [Validators.required, Validators.min(0.01)]], description: [''] });
  readonly variantForm = this.fb.nonNullable.group({ size_id: ['', Validators.required], color_id: ['', Validators.required], sku: [''] }); readonly inventoryForm = this.fb.nonNullable.group({ branch_id: ['', Validators.required], product_variant_id: ['', Validators.required], stock_quantity: [0, [Validators.required, Validators.min(0)]] }); readonly imageForm = this.fb.nonNullable.group({ is_primary: [false], sort_order: [0, [Validators.required, Validators.min(0)]] });

  constructor() { this.loadAll(); }
  get activeCities(): City[] { return this.cities.filter((item) => item.is_active); } get activeCategories(): Category[] { return this.categories.filter((item) => item.is_active); } get activeSizes(): Size[] { const sizes = this.sizes.filter((item) => item.is_active); const system = this.selectedProduct?.size_system || this.productForm.controls.size_system.value; return this.section === 'products' && (this.showProductForm || Boolean(this.selectedProduct)) ? sizes.filter((item) => item.size_type === system) : sizes; } get compatibleSizes(): Size[] { const system = this.productForm.controls.size_system.value; return this.sizes.filter((item) => item.is_active && item.size_type === system); } get activeColors(): Color[] { return this.colors.filter((item) => item.is_active); } get activeSuppliers(): Supplier[] { return this.suppliers.filter((item) => item.is_active); } get activeSeasons(): Season[] { return this.seasons.filter((item) => item.is_active); }
  get variantCount(): number { return this.products.reduce((total, product) => total + product.variant_count, 0); }
  get activeProductCount(): number { return this.products.filter((product) => product.is_active).length; }
  get branchAssignableUsers(): AdminUser[] { return this.users.filter((user) => user.role === 'BRANCH_MANAGER' || user.role === 'CASHIER'); }
  audienceLabel(audience: string): string { return ({ WOMEN: 'Mujer', MEN: 'Hombre', UNISEX: 'Unisex' } as Record<string, string>)[audience] || audience; }
  sizeTypeLabel(type: string): string { return ({ ALPHA: 'Alfabética', NUMERIC: 'Numérica', ONE_SIZE: 'Única' } as Record<string, string>)[type] || type; }
  setProductSizeSystem(system: SizeSystem): void { this.productForm.controls.size_system.setValue(system); }
  get bulkVariantInvalid(): boolean { return !this.selectedProduct || !this.variantForm.controls.color_id.value || !this.selectedSizeIds.length; }
  sizeLabel(sizeId: number): string { return this.activeSizes.find((size) => size.id === sizeId)?.name || ''; }
  isSizeSelected(sizeId: number): boolean { return this.selectedSizeIds.includes(sizeId); }
  toggleSize(sizeId: number): void { this.selectedSizeIds = this.selectedSizeIds.includes(sizeId) ? this.selectedSizeIds.filter((id) => id !== sizeId) : [...this.selectedSizeIds, sizeId]; }
  setVariantColor(value: string): void { this.variantForm.controls.color_id.setValue(value); }
  addSelectedVariants(): void { if (!this.selectedProduct || this.bulkVariantInvalid) return; const colorId = Number(this.variantForm.controls.color_id.value); const variants = this.selectedSizeIds.map((sizeId) => ({ size_id: sizeId, color_id: colorId })); this.api.addVariantsBulk(this.selectedProduct.id, variants).subscribe({ next: (product) => { this.selectedProduct = product; this.products = this.products.map((item) => item.id === product.id ? { ...item, variant_count: product.variants.length } : item); this.selectedSizeIds = []; this.variantForm.reset(); this.inventoryForm.reset({ stock_quantity: 0 }); this.error = ''; this.loadInventory(product.id); }, error: (response) => this.fail(response) }); }
  get activeUserCount(): number { return this.users.filter((user) => user.is_active).length; }
  get adminUserCount(): number { return this.users.filter((user) => user.role === 'ADMIN').length; }
  get overviewDate(): string { return new Intl.DateTimeFormat('es-BO', { day: '2-digit', month: 'short' }).format(new Date()).replace('.', '').toUpperCase(); }
  get filteredUsers(): AdminUser[] { const query = this.userQuery.trim().toLowerCase(); return this.users.filter((user) => (this.userFilter === 'all' || (this.userFilter === 'active' ? user.is_active : !user.is_active)) && (!query || `${user.first_name} ${user.last_name} ${user.email} ${user.role}`.toLowerCase().includes(query))); }
  roleLabel(role: string): string { return ({ ADMIN: 'Administrador', CLIENT: 'Cliente', CASHIER: 'Cajero', BRANCH_MANAGER: 'Encargado', SUPPLIER: 'Proveedor' } as Record<string, string>)[role] || role; }
  get filteredProducts(): ProductSummary[] { const query = this.productQuery.trim().toLowerCase(); return this.products.filter((product) => (this.productFilter === 'all' || (this.productFilter === 'active' ? product.is_active : !product.is_active)) && (!query || [product.name, product.code, product.slug, product.category_name].some((value) => value.toLowerCase().includes(query)))); }
  get activeBranches(): Branch[] { return this.branches.filter((item) => item.is_active); }
  get filteredSuppliers(): Supplier[] { const query = this.supplierQuery.trim().toLowerCase(); return this.suppliers.filter((supplier) => (this.supplierFilter === 'all' || (this.supplierFilter === 'active' ? supplier.is_active : !supplier.is_active)) && (!query || [supplier.trade_name, supplier.legal_name, supplier.tax_id, supplier.email, supplier.address].some((value) => value?.toLowerCase().includes(query)))); }
  get suppliersWithEmail(): number { return this.suppliers.filter((supplier) => Boolean(supplier.email)).length; }
  get filteredCategories(): Category[] { return this.filterMasters(this.categories, this.categoryQuery); }
  get filteredSizes(): Size[] { return this.filterMasters(this.sizes, this.sizeQuery); }
   get filteredColors(): Color[] { return this.filterMasters(this.colors, this.colorQuery); }
   get masterEditItems(): (Category | Size | Color)[] { return this.masterEditType === 'categories' ? this.categories : this.masterEditType === 'sizes' ? this.sizes : this.colors; }
  get filteredSeasons(): Season[] { const query = this.seasonQuery.trim().toLowerCase(); return this.seasons.filter((season) => (this.seasonFilter === 'all' || (this.seasonFilter === 'active' ? season.is_active : !season.is_active)) && (!query || season.name.toLowerCase().includes(query) || this.collections.some((collection) => collection.season_id === season.id && collection.name.toLowerCase().includes(query)))); }
  get filteredCollections(): Collection[] { const query = this.seasonQuery.trim().toLowerCase(); return this.collections.filter((collection) => (!this.selectedSeasonId || collection.season_id === this.selectedSeasonId) && (!query || collection.name.toLowerCase().includes(query) || collection.season_name.toLowerCase().includes(query))); }
  get selectedSeasonName(): string { return this.selectedSeasonId ? this.seasons.find((season) => season.id === this.selectedSeasonId)?.name || 'Temporada seleccionada' : 'Todas las temporadas'; }
  get filteredCities(): City[] { const query = this.locationQuery.trim().toLowerCase(); return this.cities.filter((city) => (this.locationFilter !== 'active' || city.is_active) && (this.locationFilter !== 'inactive' || !city.is_active) && (!query || city.name.toLowerCase().includes(query) || this.branches.some((branch) => branch.city_id === city.id && branch.name.toLowerCase().includes(query)))); }
  get filteredBranches(): Branch[] { const query = this.locationQuery.trim().toLowerCase(); return this.branches.filter((branch) => (this.locationFilter !== 'active' || branch.is_active) && (this.locationFilter !== 'inactive' || !branch.is_active) && (this.locationFilter !== 'branches' || branch.is_active) && (!this.selectedCityId || branch.city_id === this.selectedCityId) && (!query || branch.name.toLowerCase().includes(query) || branch.city_name.toLowerCase().includes(query) || branch.address.toLowerCase().includes(query))); }
  get selectedCityName(): string { return this.selectedCityId ? this.cities.find((city) => city.id === this.selectedCityId)?.name || 'Ciudad seleccionada' : 'Todas las ciudades'; }
  selectCity(city: City): void { this.selectedCityId = this.selectedCityId === city.id ? null : city.id; }
  branchCount(cityId: number): number { return this.branches.filter((branch) => branch.city_id === cityId).length; }
  collectionCount(seasonId: number): number { return this.collections.filter((collection) => collection.season_id === seasonId).length; }
  seasonDateRange(season: Season): string { return season.starts_on || season.ends_on ? `${season.starts_on || 'Sin inicio'} → ${season.ends_on || 'Sin fin'}` : 'Fechas por definir'; }
  private filterMasters<T extends { name: string; is_active: boolean }>(items: T[], query: string): T[] { const normalized = query.trim().toLowerCase(); return items.filter((item) => (this.masterFilter === 'all' || (this.masterFilter === 'active' ? item.is_active : !item.is_active)) && (!normalized || item.name.toLowerCase().includes(normalized))); }
  private loadAll(): void { this.api.users().subscribe((p) => this.users = p.items); this.api.cities().subscribe((p) => { this.cities = p.items; if (!this.selectedCityId) this.selectedCityId = p.items.find((city) => city.is_active)?.id || p.items[0]?.id || null; }); this.api.branches().subscribe((p) => this.branches = p.items); this.api.categories().subscribe((p) => this.categories = p.items); this.api.sizes().subscribe((p) => this.sizes = p.items); this.api.colors().subscribe((p) => this.colors = p.items); this.api.suppliers().subscribe((p) => this.suppliers = p.items); this.api.seasons().subscribe((p) => this.seasons = p.items); this.api.collections().subscribe((p) => this.collections = p.items); this.api.products().subscribe((p) => this.products = p.items); }
  private fail(response?: unknown): void { const validation = response instanceof HttpErrorResponse && Array.isArray(response.error?.detail) ? response.error.detail.map((item: { msg?: string }) => item.msg).filter(Boolean).join('. ') : ''; const detail = response instanceof HttpErrorResponse && typeof response.error?.detail?.message === 'string' ? response.error.detail.message : validation || (response instanceof HttpErrorResponse && response.status === 401 ? 'Tu sesión de administrador expiró. Vuelve a iniciar sesión.' : 'No se pudo completar la operación. Verifica los datos.'); this.error = detail; }
   createUser(): void { if (this.userForm.invalid) return; const raw = this.userForm.getRawValue(); const data = { ...raw, supplier_id: raw.supplier_id ? Number(raw.supplier_id) : null }; this.api.createUser(data).subscribe({ next: (item) => { this.users = [item, ...this.users]; this.userForm.reset({ role: 'CLIENT', supplier_id: '' }); this.showUserForm = false; }, error: (response) => this.fail(response) }); } toggleUser(item: AdminUser): void { this.api.setUserActive(item.id, !item.is_active).subscribe({ next: (u) => item.is_active = u.is_active, error: (response) => this.fail(response) }); }
   selectUserForEdit(value: string): void { this.editingUserId = value; this.editingUser = this.users.find((user) => `${user.id}` === value) || null; if (this.editingUser) this.userEditForm.patchValue({ first_name: this.editingUser.first_name, last_name: this.editingUser.last_name, phone: this.editingUser.phone, role: this.editingUser.role, supplier_id: this.editingUser.supplier_id ? `${this.editingUser.supplier_id}` : '' }); else this.userEditForm.reset({ role: 'CLIENT', supplier_id: '' }); }
   saveUserEdit(): void { if (!this.editingUser || this.userEditForm.invalid) return; const raw = this.userEditForm.getRawValue(); this.api.updateUser(this.editingUser.id, { first_name: raw.first_name, last_name: raw.last_name, phone: raw.phone, role: raw.role, supplier_id: raw.supplier_id ? Number(raw.supplier_id) : null }).subscribe({ next: (user) => { this.users = this.users.map((item) => item.id === user.id ? user : item); this.editingUser = user; this.error = ''; }, error: (response) => this.fail(response) }); }
  selectBranchAssignmentUser(value: string): void { this.branchAssignmentUserId = value; this.branchAssignmentIds = this.users.find((user) => `${user.id}` === value)?.branch_ids || []; }
  toggleBranchAssignment(id: number): void { this.branchAssignmentIds = this.branchAssignmentIds.includes(id) ? this.branchAssignmentIds.filter((item) => item !== id) : [...this.branchAssignmentIds, id]; }
  saveBranchAssignment(): void { const userId = Number(this.branchAssignmentUserId); if (!userId) return; this.api.updateUser(userId, { branch_ids: this.branchAssignmentIds }).subscribe({ next: (user) => { this.users = this.users.map((item) => item.id === user.id ? user : item); this.error = ''; }, error: (response) => this.fail(response) }); }
  createCity(): void { if (this.cityForm.invalid) return; const name = this.cityForm.getRawValue().name.trim(); if (this.cities.some((city) => city.name.trim().toLowerCase() === name.toLowerCase())) { this.error = 'La ciudad ya está registrada.'; return; } this.api.createCity(name).subscribe({ next: (i) => { this.cities = [...this.cities, i]; this.selectedCityId = i.id; this.cityForm.reset(); this.showCityForm = false; this.error = ''; }, error: (response) => this.fail(response) }); } toggleCity(i: City): void { this.api.setCityActive(i.id, !i.is_active).subscribe({ next: (u) => { i.is_active = u.is_active; if (u.is_active) this.selectedCityId = u.id; }, error: (response) => this.fail(response) }); }
  createBranch(): void { if (this.branchForm.invalid) return; const raw = this.branchForm.getRawValue(); const cityId = Number(raw.city_id); if (this.branches.some((branch) => branch.city_id === cityId && branch.name.trim().toLowerCase() === raw.name.trim().toLowerCase())) { this.error = 'Ya existe una sucursal con ese nombre en la ciudad seleccionada.'; return; } this.api.createBranch({ ...raw, city_id: cityId }).subscribe({ next: (i) => { this.branches = [...this.branches, i]; this.selectedCityId = i.city_id; this.branchForm.reset(); this.showBranchForm = false; this.error = ''; }, error: (response) => this.fail(response) }); } toggleBranch(i: Branch): void { this.api.setBranchActive(i.id, !i.is_active).subscribe({ next: (u) => i.is_active = u.is_active, error: (response) => this.fail(response) }); }
  createCategory(): void { if (this.categoryForm.invalid) return; this.api.createCategory(this.categoryForm.getRawValue()).subscribe({ next: (i) => { this.categories = [...this.categories, i]; this.categoryForm.reset(); }, error: () => this.fail() }); } createSize(): void { if (this.sizeForm.invalid) return; this.api.createSize(this.sizeForm.getRawValue()).subscribe({ next: (i) => { this.sizes = [...this.sizes, i]; this.sizeForm.reset({ size_type: 'ALPHA', sort_order: 0 }); }, error: () => this.fail() }); } createColor(): void { if (this.colorForm.invalid) return; this.api.createColor(this.colorForm.getRawValue()).subscribe({ next: (i) => { this.colors = [...this.colors, i]; this.colorForm.reset(); }, error: () => this.fail() }); }
   toggleMaster(path: 'categories' | 'sizes' | 'colors', item: Category | Size | Color): void { this.api.setMasterActive(path, item.id, !item.is_active).subscribe({ next: () => item.is_active = !item.is_active, error: () => this.fail() }); }
   selectMasterForEdit(value: string): void { this.masterEditId = value; this.editingMaster = this.masterEditItems.find((item) => `${item.id}` === value) || null; if (!this.editingMaster) { this.masterEditForm.reset({ name: '', description: '', size_type: 'ALPHA', sort_order: 0, hex_code: '' }); return; } const item = this.editingMaster; this.masterEditForm.patchValue({ name: item.name, description: 'description' in item ? item.description || '' : '', size_type: 'size_type' in item ? item.size_type : 'ALPHA', sort_order: 'sort_order' in item ? item.sort_order : 0, hex_code: 'hex_code' in item ? item.hex_code || '' : '' }); }
   saveMasterEdit(): void { if (!this.editingMaster || this.masterEditForm.invalid) return; const raw = this.masterEditForm.getRawValue(); const applyCategory = (item: Category): void => { this.categories = this.categories.map((value) => value.id === item.id ? item : value); this.editingMaster = item; this.error = ''; }; const applySize = (item: Size): void => { this.sizes = this.sizes.map((value) => value.id === item.id ? item : value); this.editingMaster = item; this.error = ''; }; const applyColor = (item: Color): void => { this.colors = this.colors.map((value) => value.id === item.id ? item : value); this.editingMaster = item; this.error = ''; }; if (this.masterEditType === 'categories') this.api.updateCategory(this.editingMaster.id, { name: raw.name, description: raw.description || null }).subscribe({ next: applyCategory, error: (response) => this.fail(response) }); if (this.masterEditType === 'sizes') this.api.updateSize(this.editingMaster.id, { name: raw.name, size_type: raw.size_type, sort_order: Number(raw.sort_order) }).subscribe({ next: applySize, error: (response) => this.fail(response) }); if (this.masterEditType === 'colors') this.api.updateColor(this.editingMaster.id, { name: raw.name, hex_code: raw.hex_code || null }).subscribe({ next: applyColor, error: (response) => this.fail(response) }); }
  createSupplier(): void { if (this.supplierForm.invalid) return; this.api.createSupplier(this.supplierForm.getRawValue()).subscribe({ next: (i) => { this.suppliers = [...this.suppliers, i]; this.supplierForm.reset(); this.showSupplierForm = false; this.error = ''; }, error: (response) => this.fail(response) }); } toggleSupplier(i: Supplier): void { this.api.setSupplierActive(i.id, !i.is_active).subscribe({ next: (u) => i.is_active = u.is_active, error: (response) => this.fail(response) }); }
  createSeason(): void { if (this.seasonForm.invalid) return; const raw = this.seasonForm.getRawValue(); this.api.createSeason({ ...raw, starts_on: raw.starts_on || null, ends_on: raw.ends_on || null }).subscribe({ next: (i) => { this.seasons = [...this.seasons, i]; this.selectedSeasonId = i.id; this.seasonForm.reset(); this.showSeasonForm = false; this.error = ''; }, error: (response) => this.fail(response) }); } toggleSeason(i: Season): void { this.api.setSeasonActive(i.id, !i.is_active).subscribe({ next: (u) => { i.is_active = u.is_active; if (u.is_active) this.selectedSeasonId = u.id; }, error: (response) => this.fail(response) }); } selectSeason(i: Season): void { this.selectedSeasonId = i.id; this.collectionForm.patchValue({ season_id: String(i.id) }); this.api.collections(i.id).subscribe({ next: (p) => this.collections = p.items, error: (response) => this.fail(response) }); }
  createCollection(): void { if (this.collectionForm.invalid) return; const raw = this.collectionForm.getRawValue(); this.api.createCollection({ ...raw, season_id: Number(raw.season_id) }).subscribe({ next: (i) => { this.collections = [...this.collections, i]; this.selectedSeasonId = i.season_id; this.collectionForm.reset(); this.showCollectionForm = false; this.error = ''; }, error: (response) => this.fail(response) }); } toggleCollection(i: Collection): void { this.api.setCollectionActive(i.id, !i.is_active).subscribe({ next: (u) => i.is_active = u.is_active, error: (response) => this.fail(response) }); }
   startProduct(): void { this.showProductForm = !this.showProductForm; this.editingProduct = null; if (this.showProductForm) { this.selectedProduct = null; this.productForm.reset({ price: 0, size_system: 'ALPHA' }); } else this.productForm.reset({ price: 0, size_system: 'ALPHA' }); } loadProductCollections(): void { const id = Number(this.productForm.controls.season_id.value); this.productCollections = id ? this.collections.filter((item) => item.season_id === id && item.is_active) : []; this.productForm.controls.collection_id.setValue(''); }
  toggleSupplierSelection(id: number): void { this.selectedSupplierIds = this.selectedSupplierIds.includes(id) ? this.selectedSupplierIds.filter((item) => item !== id) : [...this.selectedSupplierIds, id]; }
  saveProduct(): void { if (this.productForm.invalid) return; const raw = this.productForm.getRawValue(); const data = { ...raw, category_id: Number(raw.category_id), season_id: raw.season_id ? Number(raw.season_id) : null, collection_id: raw.collection_id ? Number(raw.collection_id) : null, supplier_ids: this.selectedSupplierIds, variants: [], images: [] }; const request = this.editingProduct ? this.api.updateProduct(this.editingProduct.id, data) : this.api.createProduct(data); request.subscribe({ next: (item) => { const index = this.products.findIndex((p) => p.id === item.id); this.products = index >= 0 ? this.products.map((p) => p.id === item.id ? item : p) : [item, ...this.products]; this.selectedProduct = item; this.showProductForm = false; this.editingProduct = null; }, error: () => this.fail() }); }
    selectProduct(i: ProductSummary): void { this.api.product(i.id).subscribe({ next: (p) => { this.selectedProduct = p; this.selectedSupplierIds = p.supplier_ids; this.productForm.controls.size_system.setValue(p.size_system); this.imageForm.patchValue({ sort_order: this.nextImageOrder(p) }); this.loadInventory(p.id); }, error: (response) => this.fail(response) }); } editProduct(i: ProductSummary): void { this.api.product(i.id).subscribe({ next: (p) => { this.selectedProduct = p; this.editingProduct = i; this.selectedSupplierIds = p.supplier_ids; this.productForm.patchValue({ category_id: String(p.category_id), season_id: p.season_id ? String(p.season_id) : '', collection_id: p.collection_id ? String(p.collection_id) : '', audience: p.audience, size_system: p.size_system, code: p.code, name: p.name, slug: p.slug, price: p.price, description: p.description || '' }); this.loadProductCollections(); this.loadInventory(p.id); this.showProductForm = true; }, error: (response) => this.fail(response) }); }
  toggleProduct(i: ProductSummary): void { this.api.setProductActive(i.id, !i.is_active).subscribe({ next: (u) => { i.is_active = u.is_active; if (this.selectedProduct?.id === i.id) this.selectedProduct.is_active = u.is_active; }, error: () => this.fail() }); }
     addVariant(): void { if (!this.selectedProduct || this.variantForm.invalid) return; const raw = this.variantForm.getRawValue(); this.api.addVariant(this.selectedProduct.id, { ...raw, size_id: Number(raw.size_id), color_id: Number(raw.color_id) }).subscribe({ next: (p) => { this.selectedProduct = p; this.products = this.products.map((item) => item.id === p.id ? { ...item, variant_count: p.variants.length } : item); this.variantForm.reset(); this.inventoryForm.reset({ stock_quantity: 0 }); this.error = ''; this.loadInventory(p.id); }, error: (response) => this.fail(response) }); }
    loadInventory(productId: number): void { this.api.inventory(productId).subscribe({ next: (page) => this.inventoryRows = page.items, error: () => this.inventoryRows = [] }); }
    saveInventory(): void { if (!this.selectedProduct || this.inventoryForm.invalid) return; const raw = this.inventoryForm.getRawValue(); const branchId = Number(raw.branch_id); const variantId = Number(raw.product_variant_id); const existing = this.inventoryRows.find((item) => item.branch_id === branchId && item.product_variant_id === variantId); const request = existing ? this.api.updateInventory(existing.id, Number(raw.stock_quantity)) : this.api.createInventory({ branch_id: branchId, product_variant_id: variantId, stock_quantity: Number(raw.stock_quantity) }); request.subscribe({ next: () => { this.loadInventory(this.selectedProduct!.id); this.inventoryForm.reset({ stock_quantity: 0 }); }, error: (response) => this.fail(response) }); }
   selectImageFiles(event: Event): void { const input = event.target as HTMLInputElement; this.imageFiles = Array.from(input.files || []); this.imagePreviewUrls = this.imageFiles.map((file) => URL.createObjectURL(file)); this.error = ''; }
   uploadSelectedImages(): void { if (!this.selectedProduct || !this.imageFiles.length || this.uploadingImages) return; const raw = this.imageForm.getRawValue(); if (raw.is_primary && this.selectedProduct.images.some((image) => image.is_primary)) { this.error = 'Este producto ya tiene una imagen principal. Desmárcala o cambia la imagen principal existente.'; return; } this.uploadingImages = true; this.uploadNextImage(0, Number(raw.sort_order), raw.is_primary); }
   private nextImageOrder(product: ProductDetail): number { return product.images.reduce((highest, image) => Math.max(highest, image.sort_order), -1) + 1; }
   private uploadNextImage(index: number, sortOrder: number, isPrimary: boolean): void { if (!this.selectedProduct || index >= this.imageFiles.length) { this.uploadingImages = false; this.imageFiles = []; this.imagePreviewUrls = []; this.imageForm.reset({ is_primary: false, sort_order: this.nextImageOrder(this.selectedProduct!) }); return; } this.api.uploadImage(this.selectedProduct.id, this.imageFiles[index], isPrimary && index === 0, sortOrder + index).subscribe({ next: (product) => { this.selectedProduct = product; this.uploadNextImage(index + 1, sortOrder, isPrimary); }, error: (response) => { this.uploadingImages = false; this.fail(response); } }); }
   setPrimaryImage(imageId: number): void { if (!this.selectedProduct) return; this.api.updateImage(this.selectedProduct.id, imageId, { is_primary: true }).subscribe({ next: (product) => this.selectedProduct = product, error: (response) => this.fail(response) }); }
   removeImage(imageId: number): void { if (!this.selectedProduct || !confirm('¿Eliminar esta fotografía del producto?')) return; this.api.deleteImage(this.selectedProduct.id, imageId).subscribe({ next: (product) => this.selectedProduct = product, error: (response) => this.fail(response) }); }
}
